{
  "name": "daily-distances-calculator",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "all": "npm run build && npm run deploy",
    "start": "parcel src/index.html --no-cache --no-hmr --open",
    "build": "parcel build src/index.html --no-cache --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "7.2.0",
    "gh-pages": "^2.2.0",
    "parcel-bundler": "^1.6.1"
  },
  "browserslist": [
    "last 1 Chrome version",
    "Firefox ESR",
    "last 1 safari version",
    "last 1 opera version",
    "last 1 edge version"
  ],
  "keywords": []
}
